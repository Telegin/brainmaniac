<h2>


<% if Story.where(status: 'editable').count > 0 %>
 <%= link_to 'Participate in Brainstorm', new_story_snippet_path(Story.where(status: 'editable').offset(rand(Story.where(status: 'editable').count)).first.id) %>
<% else %> 
      Participate in Brainstorm
<% end %>
</h2>